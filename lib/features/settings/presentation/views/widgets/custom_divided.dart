import 'package:flutter/material.dart';

class CustomDivided extends StatelessWidget {
  const CustomDivided({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      color: Colors.grey.withOpacity(.5),
    );
  }
}
